# M21 · 恢复公开版视觉与 Phoenix Tree Logo

2026-09-06。本地已实现并完成本轮验证；未提交、未推送、未发布。

## 用户要求与执行范围

用户要求「变回公开版本」，并以 https://phoenixtree.ai/ 的品牌 Logo 替换左上角图案。
本轮恢复的是公开 v1.0.4 的视觉基线，不把业务代码回退至旧版本：双引擎、任务入口、证据阅读、移动导航及已有修复保留。
此前 M20 蓝紫赛博样式已被本轮替代，M20 记录和预览作为历史保留。

## 来源与改动

- 公开样式直接读取 GitHub `v1.0.4:desktop/src/index.css`：深蓝黑、朱橙、大圆角、玻璃渐变及暖橙光晕恢复。
- 移除赛博网格、蓝紫边光和首页赛博介绍框；保留后继响应式布局与阅读样式，次要文字使用后继可读性令牌，不宣称逐像素复刻公开截图。
- 行情色向恢复公开版红涨绿跌；绿色不作为品牌主色。
- 官网 Logo 经实时 HTML 及真实 Chrome DOM 两次确认：原始 viewBox、transform、path 均沿用，未重新生成或描摹。
- 新增 `PhoenixTreeLogo.tsx`，以 inline SVG/currentColor 展示，随当前暖橙主题着色；不请求远端图片，无外链、脚本或图片内嵌。
- Layout 仅替换原 LineChart 图标及导入；首页链接仍为 `/`，可访问名称仍为「Vibe Research 首页」。未改产品名称、站点图标或新增公司导流。
- Home 只改一个样式类，useDarkMode 只改注释；视觉测试更新为新要求。改前文件保存在 `.local/reviews/m21-before-*`。

## 本轮验证

- 先新增回归测试：修正测试正则转义笔误后，得到预期的两项红灯（仍是赛博样式、Logo 组件不存在）；实现后前端 **62/62** 通过。
- TypeScript 与 Vite build 通过；原有大 chunk 提示保留。
- UI 边界 **12/12** 通过，`git diff --check` 通过。
- 深/浅 × 1440/390 宽 × 首页/研究页/AI 设置共 12 组布局检查：无页面或主区横向溢出，Logo DOM 正确，无赛博网格。
  首次深色桌面研究页在懒加载完成前取样，main/logo 暂未出现；改为等待 main 挂载后复核通过，不把加载中空页面算通过。
- 折叠侧栏宽 56px，Logo 24×32px，完整位于侧栏内；移动菜单宽 228px，Logo 可见、无横溢出，Esc 关闭后焦点回到「打开导航」。
- 强制高对比度模式下背景图片关闭。未进行完整 WCAG 对比度审计或全部业务页视觉验收。
- 实际 Codex CLI / GPT-5.6 Sol，一轮独立只读 CSS/TypeScript 增量审查：`No actionable P1/P2 regressions.`，记录 `.local/reviews/m21-public-logo-review.md`。

## 预览与隔离

预览：`.local/reviews/m21-public-logo-dark.png`、`m21-public-logo-light.png`、`m21-public-logo-mobile.png`。
测试使用隔离端口 5974、已有历史/合成验证数据；截图不是今日实时行情证明。未运行新研究或业务模型、未调用 WorkBuddy。
测试无密钥偏好已移除，本轮测试标签及官网核验标签已关闭，验证服务结束；正常 5930 服务、产品登录态未修改。
不改变日常分支/HEAD，不暂存。M9–M19 其他未提交改动保留；M19 云端测试不覆盖 M21。

## 交付自检

使用 frontend-design 恢复既有视觉、website-test 功能模式核验页面、codex-delegate 独立审查；按本项目规则未使用 MiMo 体验审查。
agent-self-evaluation：准确性 4（实取样式及 Logo，未做逐像素对照）；完整性 4（含折叠与窄屏，未遍历全部页）；
清晰度 4（明确视觉恢复不等于代码回退，但历史样式记录仍较多）；可操作性 5（源码和预览就绪）；
简洁度 4（为保留公开 CSS 来源，后继布局放在兼容段，少量令牌覆盖重复）。平均 4.2/5。
改善项：后续维护只以最新日志的视觉方向为准；有新增页面改动时补对应页面检查。用户视角：恢复熟悉暖橙外观并换官网图案，未牺牲新功能或擅自发布。
