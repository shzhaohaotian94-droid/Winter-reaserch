# M33 投资者体验与应用图标验收

日期：2026-09-06。范围仅活动 Research checkout，沿用 feat/dual-engine 脏工作区，不覆盖既有 M9–M32 工作。没有暂存、提交、push、tag、Release、网站部署或 Apple 公证上传。

## 用户决定

用户要求修复影响投资者体验的剩余项，并先确定安装包图案。已确认“石墨底＋橙色凤凰羽翼，轮廓呼应 V”。图片由 imagegen 生成，原始 PNG 为 1254×1254；`packaging/macos/assets/app-icon.png` 是获批母本。构建生成标准多尺寸 ICNS，应用配置及原生启动窗口使用该图标，页面左上角品牌 Logo 不变。原深浅配色、默认 Agent 关闭及所有栏目布局不变。

## 已处理的体验项

- 清仓记录：添加可选公司名称和复盘备注，写入既有真实台账。旧记录无需迁移；名称/备注留空仍合法。人民币、港币、美元及确定性盈亏算法不变。
- 清仓日期：可粘贴 YYYY-MM-DD，也保留日历入口；无效日历日期拒绝，不用浏览器自动滚动后的日期默默入账。
- 资料引用：从真实资料元数据取文件名，原始 id 仍保留；只有真实范围内的页码才展示。点击进入本地提取文本预览并可下载完整原文件；未知页码明确未核实，删除后的链接解释资料不可用。
- 对话：停止等待改为普通状态提示；等待显示实际秒数而非伪造进度。运行中可写下一问，但明确不会自动提交。首页和侧栏共用。
- 浏览器保存：配额不足或存储不可用时明确聊天未保存，提示先复制内容；超出当前二十轮保存上限时提醒重要结论另行保存。不自动删除别的聊天腾空间。
- 每日复盘：五组真实请求状态独立显示，部分返回不冒充全部完整；重复刷新不叠加请求。刷新开始撤下旧业务日期，旧指数若暂存会明确提醒。
- 预览十万字符与索引百万字符不是同一业务限制，保留各自边界；不为了“统一常量”削减索引或放大预览。用户有完整原文下载入口。

## 验证与审查

后端完整回归：831 项，830 通过、1 项 Windows 专项在 Mac 跳过。前端最终完整回归：77/77；构建包含类型检查。没有 Python 代码变更，本轮不声称重跑历史 Python 737 项。

实际 Codex CLI / GPT-5.6 Sol 独立静态复审三轮：R1 无 P1/P2；后加刷新进度后的 R2 找到旧 pageMeta 未清理的 P2；修复并补“请求开始就清理旧日期 / 重复刷新不叠加 / 完成后解锁”的行为回归，R3 无可操作 P1/P2。不是 Claude 原始全仓审计重跑，也不把静态审查写成运行验收。

使用 website-test 的功能验收流程和真实 Chrome，在生产静态服务 5979、独立数据 `/tmp/vra-m33-qa-n0B8Eq` 上检查：清仓名称/备注/盈亏保存和刷新持久化、合成研报真实上传、文件名引用、点击预览、关闭清 query、删除、失效引用说明、五组取数状态、无效日期、日历入口、模拟浏览器存储配额失败提示均通过。引用聊天是明确标注的合成 UI 数据，不是模型生成证据。原文下载先验证入口，不把按钮在场当完整文件下载验收。

失败记录保留：初次日期自动化缺少代码字段，因此先被代码校验挡住，补齐后无效日期检查通过；旧全局指数回归的宽正则误匹配新增状态表达式，缩窄为 JSX 面板显示条件；测试夹具增加新 ref/setter，不删除失败路径。第一次官方 Node 下载超时保留失败目录，重建使用已校验官方归档。用户真实台账及浏览器 5940 未动。

## 尚待收口

最终安装包迁移与版本替换、签名/包内文件复核、当前源码完整研究的结果在本节后补。其他厂商密钥、Windows 实体机、Intel、最低系统逐版、外部干净 Mac、Gatekeeper 正式签名/公证、全部数据源恢复不能由 Mac 单机静态回归代替。全球指数仍有未接到底座的入口，会明确显示不可用；没有编造行情填补。

## 安装包与升级验证（20:56）

最终构建 `.local/mac-builds/20260906T124608/`，DMG 约 374 MiB，SHA256 `d9cb176d4cc35b4990a5cddb012a7ec231520fb535c2723c631aee4b158596fa`。交付位于父目录 `Mac安装包_M33_2026-09-06_v1/`，M32 保留。

1. 等构建完成后 hdiutil 完整校验通过；只读挂载，复制应用，卸载镜像。首个 verify 在 hdiutil 仍压缩镜像时提前执行，返回资源暂不可用，属于验收时序问题而非包损坏；没有改签名要求追绿。
2. 225 源文件哈希逐项一致，28 前端产物与当前 dist 一致；17092 文件、15 个包内链接，私有文件路径命中 0、包外链接 0。AppIcon.icns 哈希与 manifest 一致，Info.plist 指向 AppIcon。构建、迁移和运行后严格 codesign 检查通过。
3. Gitleaks 脱敏扫描约 76.98 MB，3 项命中均现场核实：东财公开查询 token、兼容矩阵测试约定词、V8 整数常量；没有发现真实凭据，不扩张为能识别所有自由文本隐私。
4. M32 在 `/tmp/vra-m33-upgrade-TXsPXY` 新建一条合成清仓和一份 17 字节合成研报，退出后从 DMG 迁移的 M33 使用同一数据目录，id/日期/数值/文档元数据保留，真实下载仍为 `M33 upgrade test.`。第一次把应用放在数据根内部，被既有指令资产包含保护拒绝；将应用移至独立 `/tmp/vra-m33-install-M0OPDk/` 后通过，没有放宽安全边界。
5. 实际原生 open→5938 health 200，退出后端口释放，迁移位置后再 open 仍通过。真实 Chrome 首页保留此前 M32 的 Claude 订阅选择和 PE 测试聊天，Agent 仍关闭；本轮没有再次调用 Claude 业务模型或 WorkBuddy。网页截图 `.local/m33-installed-home.png` 已目视检查。
6. 安装版并非完整离线验收，也没有 Apple Developer ID 签名/公证。升级验证只覆盖本次具体清仓/资料样本，不等于所有未来迁移。临时数据与失败构建保留作证据，未删除用户真实数据。

## 当前源码六阶段真实运行（21:02 收口）

`m33-gpt56-full-2`：300308.SZ，显式 GPT-5.6 Sol / 产品专用 Codex 登录，`endpoint_scope=full`，20:39:21→21:02:13，1372.633 秒（22分53秒）。六阶段均一次 complete，validator_ok 全真，进程 exit 0；994 条证据、20 项计算且20项均 ok，12个报告二级章节，归档 `knowledge/companies/SZ_300308/runs/m33-gpt56-full-2.md` 与 latest 在场。gate 无命中，hooks71次/错误0；诊断日志不是独立可信安全证明。

本标的本次适用取数账本39项：36 ok、2 failed（sw_industry / us_anchor_earnings）、1 timeout（macro_probability）。full 表示全阶段计划及适用扩展来源，不等于把注册表每个端点、所有市场全部实测。申万 TLS 校验失败、Nasdaq 明确尚无日期、宏观概率180秒超时均保留，没有关闭 TLS 或篡改 failed 来变绿。

这是当前源代码通过产品引擎的完整研究，不是安装版全部长业务验收，更不证明所有模型、长回测、辩论、Windows 或所有资料源可用。该结果补齐了“最终源码未完整跑一次”的一个缺口。打包后只改开发/验收文档，运行源及前端无漂移。

## 自检（agent-self-evaluation）

| 维度 | 分数 | 证据与改进 |
|---|---|---|
| 准确性 | 4/5 | 回归、三轮复审、真实运行及升级均有产物；仍不能证明全部外部源数字，继续保持来源范围标注 |
| 完整性 | 3/5 | 图标和主要P3体验闭环，但全球指数、全厂商、外部干净机器与安装版长业务未闭环；后续按投资者路径逐项补验 |
| 清晰性 | 4/5 | 把源码、安装、外部源、发布状态分开记录；交付页保持短，不要求用户读全部日志 |
| 可操作性 | 4/5 | 已有DMG、SHA和步骤；正式客户分发还需签名/公证，不能用本地成功替代 |
| 简洁性 | 4/5 | 用户交付简短，细节归档；长日志留证据，不在最终答复重复 |

平均3.8/5。无≤2项。最高优先后续：未接入/不稳定数据路径；安装版长业务及外部干净Mac；正式签名公证另需授权。用户应同意“本地可测试、不等于所有缺口消失”的边界，因此交付本地测试包而非宣称全量发行完成。
