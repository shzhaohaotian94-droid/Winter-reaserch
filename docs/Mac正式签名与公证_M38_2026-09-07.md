# M38 · Mac 正式签名与 Apple 公证

## 授权与范围

2026-09-07 Simon 确认使用现有证书签名并上传 Apple 公证；不公开发布。
本轮不暂存、提交、push、tag、Release 或部署。不更新全局记忆，不导出私钥，不复制个人 Codex 登录。
源为 M37 最终 v3 对应 `.local/mac-builds/20260906T173234/image/VibeResearch.app`，复制后签名，原包保留。
M38 是分发验证批次，应用业务版本仍 1.0.4/build37；没有把历史测试数冒充本轮重跑。

## 已验证 · 07:39 本地时间

- Developer ID Application: Sizhen Lin，Team W23NY49KQ5；有效证书 SHA1
  `04AB98C9B4AA0ACECA9F901D58C31019077106C2`。只通过系统签名服务调用私钥。
- `.local/m38-sign-v1/VibeResearch.app` 内 94 个 Mach-O 全部逐个签名、严格验签，主 App 最后封装。
  逐个核对 Team、secure timestamp、runtime flag，0 个异常；无 get-task-allow/DYLD/disable-library-validation。
- Node 与 code-mode-host 仅保留 allow-jit/allow-unsigned-executable-memory，其余不加例外。
  原 Node 捆绑版本带调试与多项额外权限，不能直接原样继承。
- 签名后 Python 七类业务依赖真实导入、ctypes 回调、Node 循环/JIT、Codex0.153.4版本调用通过。
- 原生 App 窗口加载，保留 Codex 来源与旧聊天；实际 Agent 调产品计算工具，合成100→121/2年返回10.00%。
  恢复 Agent OFF，运行之后严格签名仍通过；截图 `.local/m38-signed-agent-calc.png`。
- 新签名脚本边界测试4/4；实际 Codex CLI / GPT-5.6 Sol 一轮独立静态复审无可行动 P1/P2，
  `.local/m38-sign-review-r1.md` 与对应完整日志保留（含 tokens used）。没有调用 WorkBuddy 或 MiMo。
- 原始 M37 build-manifest SHA256 `8acac38a2eaafdc5b7e7c7007e198d4284753758b015e4c3e9394c24d01fe76d`。
  签名清单、脚本哈希、原生文件列表见 `.local/m38-sign-v1/signing-record.json`。

## Apple 公证状态 · 已取得票据与 Gatekeeper 接受

未找到预存 notarytool profile；未索取或读取密码/私钥正文。
旧 App Store 项目记录指向 Xcode 已有 Apple 登录，因此使用本机 Xcode 官方 Developer ID 上传通路。
`xcodebuild -exportArchive` 明确 `method=developer-id`、`destination=upload`、manual、指定 Team/证书，
不自动管理版本、不开自动创建证书/描述文件。归档副本在 `.local/m38-xcode/`，原签名候选不动。
已进入 Apple notary service 上传；不是 App Store Connect 上架，也不是公开分发。
日志 `.local/m38-xcode-notary.log`；Xcode 原始 distribution logs 留本机临时目录，不公开复制其中认证元数据。

07:40:41 上传成功，任务 `242B86A9-6301-4249-BBEC-B89DCAE02CCD`，首次两次导出因处理中拒绝。
07:44 左右，Apple stapler 下载票据并装订成功，独立 validate 成功；
Gatekeeper 实际返回 `accepted / source=Notarized Developer ID`。
第三次 `xcodebuild -exportNotarizedApp` 返回 EXPORT SUCCEEDED，输出 `.local/m38-notarized-export/VibeResearch.app`，
该导出 App 再次通过 stapler validate 和严格验签。没有把原签名候选冒称最终导出物。
Xcode 原归档 Info 的 processingEvent 仍缓存为 processing，不能只读这个缓存字段判定远端状态；
最终完成证据是可验证的 Apple 票据、正式导出成功和 Gatekeeper 判词，没有伪造或回填归档状态。

导出 App CDHash：`28e5bbd12b5ad999e60d28fcf2a494ead779162f`。
日志：`.local/m38-staple-app-r1.log`、`.local/m38-export-notarized-r3.log`、`.local/m38-gatekeeper-export.log`。
最终 DMG 为 Developer ID 签名容器，包含已公证且带票据的 App；本次没有对 DMG 另行上传公证，
不声明 DMG 本身具有独立公证票据。两种状态在安装说明中明确区分。
最终 DMG 复制安装、启动与运行后完整性复验已通过，详见下节；公证不代表任意外部机器已经人工验收。

## 最终 DMG 安装验收与交付 · 07:54 本地时间

- DMG 通过严格签名校验与 `hdiutil verify`。只读挂载后复制 App 到
  `.local/M38 最终安装验证/VibeResearch.app`，卸载镜像，再从该副本启动独立窗口。
- 安装副本通过 `stapler validate`、`codesign --verify --deep --strict`，
  Gatekeeper 返回 `accepted / source=Notarized Developer ID`。
- 最终副本真实 Agent 工具调用：合成100→144、2年，计算返回20.00%，并显示实际工具记录。
  截图 `.local/m38-final-notarized-agent-calc.jpeg`；未联网取数或启动长研究。
- 恢复 Agent OFF，正常 Cmd+Q 后5938无监听；退出后上述三项校验再次通过。
  重启恢复 Codex订阅来源、Agent OFF、旧聊天及新增20.00%结果。没有删除客户数据或复制个人凭据。
- 安装包复制（非移动）到父目录 `Mac安装包_M38_2026-09-07_v1/`，
  文件名 `VibeResearch-1.0.4-M38-mac-arm64.dmg`，约370MiB；原始产物与历史包均保留。
  SHA256：`88c2acfc9aa268c85363618ce247fff562d69db934786270ecea38d0edd4a8ff`。
  同目录校验文件实跑返回 OK，并附使用说明。
- HEAD 仍为 `2ed80acd5bfa976fc966fe43bcd5b008b08c9d09`，索引为空；本轮未提交或公开发布。

## 收尾自检

结论：本轮签名、公证与本机安装闭环完成，质量自评4.0/5，不扩称全面发布验收完成。

| 维度 | 分数 | 证据与改进点 |
| --- | --- | --- |
| 准确性 | 4 | 以真实票据、Gatekeeper及运行后验签为证；归档缓存状态仍需与最终证据区分。 |
| 完整性 | 4 | App签名/公证/装订、DMG、安装工具调用及重启已验；尚非另一台干净Mac的安装验证。 |
| 清晰度 | 4 | 安装说明区分App公证与DMG签名；App内历史NOTICE需结合本批验收理解。 |
| 可执行性 | 4 | 可直接打开DMG安装，附哈希；供应商接入仍要求客户自己的有效订阅/API。 |
| 简洁性 | 4 | 简短安装说明与详细技术记录分离；原始日志仍分散保留以便追溯失败。 |

改进优先级：公开分发前在另一台Mac验证首次安装；下一次重新构建时把构建状态与分发状态元数据明确分层，
不要改写已封签App。自查：用户能直接试用，同时看到尚未覆盖的范围；不以自评分替代客观验收。

## 依据与决策

采用 Apple 系统 codesign、ditto、Xcode/notarytool/stapler，而非新增 Electron 依赖。
已查看 npm @electron/osx-sign 2.7.0（Electron 专用，不适用于此 AppKit + Python 结构），
新增仅为项目签名顺序/权限/输入输出边界的薄包装。

- [Apple 自定义公证工作流](https://developer.apple.com/documentation/security/customizing-the-notarization-workflow)
- [Apple 公证前要求](https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution)

保留 M37 的外部机器、Windows/Intel、完整供应商矩阵、外部数据源与 Node26 并发测试等待超时范围。
Apple 公证通过也不等于这些功能验收缺口已关闭。
