# M36 · 数据可用性与安装版验收

## 范围与状态

Simon 授权推进剩余客户体验、安装版业务与验证缺口。本轮先修复全球指数接入、宏观概率部分结果丢失，以及审计发现的复盘快照竞态。仅本地开发与 Apple Silicon 测试包；不暂存、提交、推送、公开发布、Developer ID 签名或 Apple 公证上传。旧 M35 包保留，个人登录态与用户资料不复制。

## 实现

- 每日复盘复用既有腾讯批量行情端点，A 股四指数与美港四指数分开请求、分区展示。海外卡片保留缺项，用“—”与缺口提示，不拿零充数。
- 海外指数显示“腾讯行情快照 · 非逐笔实时行情”、价格证据的取数时间；AI 页面上下文同时带取数时间、价格证据与缺口。取数时间不是交易所报价时间，不宣称实时行情已验证。
- 宏观概率每来源设 65 秒协作式预算，单次套接字等待至多 10 秒，逐块读取时检查预算。DNS 与进程级异常仍依靠外层 180 秒终止，不宣称严格墙钟保证。
- 后续分页网络/JSON/顶层形状失败保留之前成功页，明确标为 partial；首个请求失败仍明确失败。所有系列发现响应单独留原始证据。
- 独立审计发现复盘可在页面尚未加载完时发起：现五组请求全部结束后才开放复盘；刷新当帧也有同步防护，生成期间不刷新基础快照。已结束但失败的数据以缺口进入提示词，不要求所有源成功才能使用。
- 安装包里程碑 M36 与 bundle build 36 共用一个版本变量；产品公开版本号仍为 1.0.4，不代表已发布本地改动。

## 验证证据

全部日志在活动仓库 `.local/`，不入公开包。

| 检查 | 本轮结果 | 证据 |
| --- | --- | --- |
| Python 全套 | 748 通过，14.38 秒 | `m36-python-tests.log` |
| 前端全套 | 82 通过 | `m36-desktop-tests.log` |
| 前端生产构建 | 通过；保留既有大 chunk 警告 | `m36-desktop-build.log` |
| 后端 Node 22 覆盖率全套 | 833 项：832 通过、0 失败、1 Windows ACL 平台跳过；71.95 秒 | `m36-node22-baseline.log` |
| 宏观概率真实取数 | 18 条证据、partial、退出 2；采样范围未全覆盖；本轮未整体超时 | `m36-macro-live-1/fetch/macro_probability.json` |
| 腾讯四海外指数真实取数 | 四个价格与涨跌幅返回，ok、退出 0 | `m36-global-live/fetch/tx_quotes_batch.json` |
| 真实 Chrome 页面 | 四卡、取数时间、快照提示与无横向溢出均通过 | `m36-global-browser.png` |
| 浏览器缺项模拟 | 页面内临时移除恒生科技证据，缺口可见、其余卡保留；随后撤销注入并刷新 | 本轮浏览器工具输出 |
| 复盘加载保护 | 刷新后“等待本轮数据…”按钮不可点；回调红测先证明旧实现会发请求 | `m36-review-guard-red.log`、前端全套 |
| 实际 Codex CLI 独立复审 | 第一轮 1 P2，核实修复；第二轮 No actionable P1/P2 findings | `m36-review.md`、`m36-review-r2.md` |

宏观预算与失败页保留由合成测试验证；本次真实联网没有触发预算耗尽，不能将该次实跑描述为“真实超时恢复”证明。后端 Node 22 一次全绿不能证明 M35 Node 26 并发偶发失败根因已消除。

## 本轮早期未关闭项（最终状态见末尾）

1. 申万官网原 XLS 地址本轮 HEAD 返回 HTTP 502；既有 TLS 故障不能说已修。未关闭证书校验，未用其他分类表冒充申万历史。
2. Nasdaq 未提供下一财报日仍为未知，不能补造日期。
3. 安装版完整六阶段、回测、多空辩论与取消/归档业务链，需本轮安装验证记录后才能关闭。
4. 路径含空格的既有研究执行约束仍在；干净外部 Mac、Intel、Windows 实机没有验收依据。
5. Node 26 并发测试偶发超时仍需定位；当前只证明捆绑 Node 22 的本轮基线通过。
6. 正式分发签名/公证/公开发布另行授权，不能拿本地完整性签名代替。

## 安装包

首包构建：`.local/mac-builds/20260906T154856/`，DMG SHA256 `e3c1415a6adbc71859eb91d1e624cbac177df27f0af11177f28ef92d410ebb87`。已只读挂载复制、卸载后启动；225 源文件哈希匹配，严格 ad-hoc 完整性签名通过。SDK/引擎 0.153.4，Node 22.23.2，Python 3.12.13。升级后 Codex 登录与原聊天保留。

### 2026-09-07 跨午夜安装实跑

- 原生 App 回测实际取得 600519 的 2025 年日线 243 条，20/60 均线策略生成报告并自动归档。工具不支持用户自定义所有费用/成交参数，先明确限制后按内置口径测试。100000 元初始资金这轮零成交，不能当作策略有效性证明；只证明真实数据到回测报告及自动归档路径。
- 原生完整研究 `20260906155219.-300308-6eab4b8b` 在读取 Git 版本号时阻塞，未到首份 manifest、零证据。安装包无 `.git`，却沿祖先目录寻找开发仓库；版本元数据不应成为业务启动依赖。
- 修复：安装源码归档不调用 Git；真实 checkout 的版本查询限制 2 秒、非交互并 SIGKILL 超时退出。失败明确记 `unknown(git-unavailable)`，源码归档记 `source-archive(no-git)`。新增 2 项回归通过、后端类型检查通过；第三轮实际 Codex 独立复审为 No actionable P1/P2 findings。
- 原生取消按钮先进入等待取消，因同步 Git 阻塞未自行退出；核对本轮 Git 子进程后精确结束它，研究随后显示“已取消”、零证据零计算。不把这次人工解阻当作取消全链路自动通过。
- 将在新的时间戳构建目录重建 M36 第二包（v2），保留首包与上述失败证据；首包不作为最终交付。后端完整回归正在重跑，以下验收状态以追加实证为准。

### M36 第二包 / v2

- 构建目录 `.local/mac-builds/20260906T160116/`；DMG SHA256 `57543e09ab4c06ec98eec5ef792aeb31338d7e33de1c88e770facd5f0ac137d4`。仍属 M36/build36，用独立时间戳与 v2 安装目录区分，不覆盖第一包。
- 从 DMG 只读挂载复制到 `.local/m36v2-installed/VibeResearch.app`，卸载后启动。226 源文件哈希一致，包含 Git 修复；严格 ad-hoc 签名通过，SDK/CLI 0.153.4。
- Git 修复后的后端完整回归 835 项：834 通过，0 失败，1 Windows 专项跳过，75.71 秒，证据 `m36-node22-final.log`。
- 原生完整研究 `20260906160601.-300308-1d09f373` 在 00:06:03 创建 manifest，`repo_version=source-archive(no-git)`；12 秒首阶段取数完成，7 源中 6 成功、申万失败。启动阻塞的安装集成复验通过，完整研究尚在运行。
- 同时从原生多空辩论页启动 600519 一轮三角色验收，来源沿用 Codex 订阅；结束结果后续追加。
- 回测归档离页返回并展开原报告，243 根日线、费用/复权/零成交限制完整保留。没有删除验收记录。

### 辩论正式输出与测试竞态

- 原生一轮辩论完成并归档，但空方为英文写作草稿。只读取本轮会话的输出元数据确认上游将其标成 `final_answer`，不是界面混入 reasoning。保留失败样本，不将该轮算作中文辩论验收通过。
- 金融插件声明可选 `outputLanguage=zh`；Core 按声明筛选正文语言，剥除证据 id/URL 后检查汉字与拉丁字符比例。提示正式中文陈述；不符合则全新会话最多重试一次，仍失败则本段空白并明确错误，不能进入后续角色上下文。取消前后检查保留。不宣称这个启发式能识别所有思考草稿，也不限制其他插件选择其他语言（不声明则沿用原行为）。
- 实际 Codex R4 指出短输出例外会放行少量汉字混入的英文草稿，已去掉例外并加短稿回归；R5 无可操作 P1/P2。覆盖重试次数上限、无草稿向后传播、证据密集中文、英文术语及取消不重试。专项 55 项通过。
- 新一次后端并发全量曾 837 项中 2 失败：新测试空底稿夹具先触发既有 no_dossier（已修夹具），旧取消测试等待 PID 消失超时。定位测试中 `existsSync` 可看到尚未写入的 PID 文件，`Number(空串)=0` 会转成进程组查询；Python/Node 两份夹具均改临时文件原子改名发布 PID，读取拒绝零/非法值，保留原等待与退出断言。3 项专项通过，实际 Codex R6 无可操作 P1/P2。旧失败未记录 PID，故不能宣称这一处已证明是全部偶发超时根因。
- 第二包原生完整研究已完成画像/财务/一致预期/估值并进入风险阶段，宏观概率本轮真实保留 18 条 partial 证据，无 errors，不是原来整次超时丢结果。切页再从归档点击运行可恢复阶段；Agent 已恢复 OFF，后台研究继续。
- 正在重新全量回归并构建 M36 第三包/v3，第二包不含辩论正文筛选，最终交付须以 v3 验收为准。

### 00:21–00:28 收口实证（替代以上进行中状态）

- v2 完整研究于 00:21:24 结束，用时 920.853 秒（约 15 分 21 秒），1005 条证据、20 项计算。六阶段全部一次通过各自校验，最终 `incomplete`、`final_errors=[]`：画像、风险、成稿如实保留资料缺口，其余三阶段 complete。hooks 61 次、2 次 Stop 拦截、0 错误、0 强制终止。不是“数据全部完整”。
- 39 个取数端点中宏观概率保留 18 条 partial 证据，无 errors；申万分类、Nasdaq 下一日期和产能/认证等证据缺口均写入最终报告。原生界面显示“资料不完整”，报告和附录可读；点击 `calc-b000a19aabb0d8da` 后焦点跳至对应附录行。报告、附录、viewer、证据文件与知识库归档/latest 均存在。
- 正常退出 v2 后启动 v3，原 Codex 登录、首页历史、Agent OFF 均保留。辩论在 OFF 时明确提示需要开启 Agent；开启后才启动三角色复验，不是绕过开关。切换 OFF 不影响已经开始的完整研究，和启动前的功能要求是不同语义。
- 最新 Node 22 全量：838 项，837 通过、0 失败、1 Windows ACL 跳过，74.04 秒；Node 26 全量同为 837/0/1，88.79 秒。证据 `m36-node22-final-v3.log`、`m36-node26-final-v3.log`；类型检查 `m36-typecheck-final-v3.log` 通过。两次当前全绿不证明历史所有偶发失败根因已定位。Node 26 行覆盖 87.11%、分支 68.74%、函数 26.24%，不声称所有维度达到 80%。
- v3 构建目录 `.local/mac-builds/20260906T161644/`；DMG SHA256 `b19ce0db4401e13cd5e6ae18c3efaac4192bc09af110aa6b22e4bac750a7bf16`。只读挂载复制至 `.local/m36v3-installed/VibeResearch.app` 后卸载，226 源文件哈希全部一致、包含辩论新保护，严格 ad-hoc 签名通过。原生 v3 后续辩论与自动取消结果继续追加。

### v3 最终本地验收与交付

- 600519 一轮三角色辩论于 00:28 完成并自动归档，多方、空方、主持均为中文，原生界面跨重启可展开。多方 17/17、空方 15/16、主持 13/13 数字对上底稿，空方 1 个未核对数字明确提示；空方还有一段先写错比较、后自行更正。主持最终采用收入同比增长的正确方向。语言保护通过，不等于输出语义全部正确；这次没有证据表明触发了格式重试，重试分支仍以合成回归为准。
- v3 原生取消运行 `20260906162858.-600519-71788115`：取数时点击“取消研究”，先显示等待后台停止，再显示“已取消”。已确认本轮研究 PID 80260 与 Python 80276 自行消失，未手工 kill；日志为“用户取消研究”，0 证据、0 计算。与首包 Git 卡死的人工解阻严格区分。
- 正常 Cmd+Q 退出并重启，产品 Codex 登录、首页已有聊天、Agent OFF、新辩论归档保留；已返回首页。未清理任何原资料或失败验收记录。
- 最终包复制到父目录 `Mac安装包_M36_2026-09-07_v3/`，含 DMG、SHA256、使用说明，复制后哈希一致。旧 M32–M35 及 M36 首包/v2 保留。当前只有本地开发与测试包，没有暂存、提交、推送或公开发布。
- 本轮实际 Codex CLI 共六轮定向独立审查：R1 复盘竞态、R4 短英文输出例外已修；R2/R3/R5/R6 各自范围无可操作 P1/P2。不是重新全仓盲审，也不替代后续发布前整体审计。

## 最终剩余门槛

1. 数据：申万源 502/TLS 问题及 Nasdaq 未提供日期仍显式缺口；不能用假数据或关闭证书验证掩盖。完整研究是 honest incomplete，不是完整数据验收。
2. 模型与回测：中文辩论仍可能比较错误，已有提示与主持复核并非正确性保证；这次零成交回测没有覆盖真实成交/费用效果。格式重试、网络预算失败恢复有自动化测试，但本次正常联网未实际触发全部分支。
3. 兼容：含空格路径约束尚未消除，Intel、Windows、外部干净 Mac、全离线与跨机器升级未实机验证。Node 22/26 当前全绿不能证明历史全部并发偶发原因已定位；Python 依赖尚非跨时间完全锁定。
4. 分发：Developer ID 签名、Apple 公证上传、公开仓库提交/推送/Release 均未执行，须另行授权。本地 ad-hoc 完整性签名不是正式分发凭据。

## 交付自检（agent-self-evaluation）

总体 3.8/5：本轮本地可交付，不能称全部外部验收完成。

| 维度 | 分数 | 证据与改进 |
| --- | --- | --- |
| 准确性 | 4 | 双 Node 全套、82 前端、748 Python 与原生路径有实证；模型仍有比较错误，不把检查器视为真理。 |
| 完整性 | 3 | 已补数据接线、部分结果保护、安装启动/辩论/取消/归档；跨机器与外部源门槛保留，下一步需这些环境证据，不能以本机代替。 |
| 清晰度 | 4 | 结果、失败样本、包身份均分列；日志早期数字保留为时间线，末节给唯一最终口径。 |
| 可操作性 | 4 | 独立 DMG、哈希、使用说明在同一目录；未公证且路径含空格受限，仍不适合陌生客户直接分发。 |
| 简洁性 | 4 | 用户交付摘要短，细节集中本页；三次包构建记录较长但为保留可追溯证据。 |

关键低分（≤2）：无。优先改进：先补外部干净安装/平台证据，再闭合源故障与模型语义质量；这些不是 30 秒可消除的缺口，需要独立后续工作。自问用户会否认同：应会认同本机流程跑通，但不能期待他把未公证测试包当作完整版公开发行。交付判定：本地测试包可交付，公开发布不可。
