# M19 独立测试分支与 Windows 云端验收

## 最终状态（2026-09-06）

第五轮云端矩阵 **6/6 success**，headSha 与独立测试分支 `1734fbe` 一致。
本地最终源码相对独立 index 无差异；M19 验收文档保留本地，不为文档再触发一轮代码 CI。

| 环境（各跑 Node 22 / 24） | 后端范围与结果 | 前端 / Python |
| --- | --- | --- |
| Windows Server 2025 云端 runner | 原生契约 73 项：72 通过、1 POSIX 中间态专项跳过；NTFS ACL 实跑通过 | 61/61、构建通过；699/699 |
| macOS 云端 runner | 全量 813 项：812 通过、1 Windows ACL 专项跳过 | 61/61、构建通过；699/699 |
| Ubuntu 云端 runner | 全量 813 项：812 通过、1 Windows ACL 专项跳过 | 61/61、构建通过；699/699 |

六组合后端类型检查通过，Windows PowerShell 入口解析通过；Python 分项为 calc 220、backtest 209、数据脚本 270。
本机最终后端同为 813 项（812 通过、1 平台跳过），类型检查通过。构建既有包体积警告不当作错误或已优化。
这不是“Windows 后端全部 813 项均跑过”，也不是 Windows 11 客户实机或真实订阅研究验收。

最终远端 main 仍 `09e8404a33ba0d05e036e01207be4701c61d692c`，开放 Secret Scanning 告警 0。
真实开发分支仍 `feat/dual-engine @ 2ed80ac`、原 index 无暂存变更，所有既有开发修改保留。
仅独立分支上传，未建 PR、未合并 main、未 tag / Release / 部署，未更新全局记忆或配置。
未调用 WorkBuddy 或 Claude 的真实模型；独立审查使用指定 Codex 订阅通道。

## 授权与代码边界

Simon 在“允许上传独立测试分支，不合并、不发布”的具体询问后回复“继续”。
本批仅做该分支的验证及发现问题的必要修复，不创建 PR、不合并 main、不打标签、不创建 Release、不部署网站。
公开测试分支可被仓库访问者看到，但默认 main 未改变，不能称为正式产品发布。

- 唯一活动源码：`vibe-research-agent-Codex`。
- 日常开发仍为 `feat/dual-engine @ 2ed80ac`，原 index 不变，M9–M18 工作区修改保留。
- 使用独立临时 Git index 建立完整候选源码快照，不将真实运行资料、凭据或 ignored 辅助脚本纳入。
- 独立分支：`ci/windows-m19-20260906`。
- 首次上传提交：`d8f1d1379a7c1c25c86ce0516cfe3f33d9fc2bd4`。
- 上传后重新查询 main：仍为 `09e8404a33ba0d05e036e01207be4701c61d692c`。
- 作者使用 GitHub noreply 邮箱。未复制或上传产品/个人登录态。

## 上传前检查与修复

Gitleaks 8.30.1 扫描候选工作树、HEAD、完整 `--all` Git 历史，分别有 20 / 20 / 23 个命中。
逐条核实为测试假密钥、脱敏金丝雀、多轮约定词与东财公开搜索参数，未发现真实凭据；不是“扫描零命中”。
Git 历史相关路径查无 .local / auth.json / .env / 私钥文件，候选新增文件没有截图或二进制资料。
产品 auth.json 保持 ignored / untracked / 0600；远端 Secret Scanning、Push Protection 开启，开放告警为 0。

候选 workflow 是唯一的 Cross-platform verification：只读 contents 权限，未注入个人 secrets，
没有发布、部署、tag 或 Release 步骤。不修改 Issues / PR / Release 正文，也不上传额外测试 artifact。

实际 Codex CLI 对未提交源码做上传前独立审查，发现一个 P2：用户取消后，research.cancelled 与
research.finished 的状态不一致，后者固定 failed。先补真实事件文件断言，确认旧代码给出 failed，
再统一复用取消判定并将结束事件记 cancelled；磁盘既有 failed + cancelled=true 契约及退出码 3 保持不变。
后端服务原本就会将该标记映射为用户可见的 cancelled，未扩大 schema 或改动正常失败语义。

48 项编排/取消定向回归通过，后端类型检查通过；实际独立增量复审结论为
`No actionable P1/P2 regressions.`。上传前再次扫描候选源码，仍是相同 20 项已核实命中。
测试开发时首个断言误读 FakeRunner 的简略日志字段，已改为读取真实 events.jsonl；只有第二次
红灯（actual failed / expected cancelled）计为修复前复现。首次 HEAD 导出参数不兼容本机 tar，
重导出后扫描才计入证据，不把工具失败记成通过。

本地证据：`.local/reviews/m19-upload-gate.md`、`m19-*-secrets*.json`、
`m19-upload-review.md`、`m19-cancel-review.md`、`m19-cancel-red-r2.log`、`m19-cancel-green.log`。

## 云端执行

首轮：[Actions 34008187251](https://github.com/simonlin1212/Vibe-Research/actions/runs/34008187251)，
headSha 与上述提交一致，六个组合均失败，完整失败日志保留在本地。

首轮定位：统一任务测试写死本机 `../.venv/bin/python`，四个 Unix 组合两项计算测试失败；
macOS Node 22 另出现派生进程结束后事件循环提前退出，等待终止的 Promise 尚未兑现。
Windows 的订阅 CLI 替身、临时目录清理及 NTFS ACL 测试失败，仍需原生错误输出定位。
不跳过这些失败测试，也不把其他通过项汇总成整体通过。

第二轮 `07e5d84c9a89736249b6ca9a418fceb1d38b7f9b`：测试 Python 改为优先尊重
`VRA_PYTHON`；两个同步等待收尾路径的四个有界计时器保持事件循环引用。
加入仅 Windows GitHub Actions 可运行的合成诊断（虚构 CLI 参数/退出码和测试文件 ACL），
不调用真实订阅程序，不读取任何登录态。31 项本地定向测试与类型检查通过。
独立增量复审曾报 Node 不支持直接导入 TS；核对 workflow 使用最新 Node 22/24，
两者默认启用类型剥离，且既有测试本就直接运行 TS，因此该项不成立。提供完整 workflow
复审后收口 `No actionable P1/P2 regressions`。扫描仍为同一 20 项已核实命中。
第二轮：[Actions 34008594667](https://github.com/simonlin1212/Vibe-Research/actions/runs/34008594667)，
四个 Unix 组合全部通过，两个 Windows 组合仍失败。

第三轮仅增强合成诊断，提交 `ef2fbb17fd145b103f5ce0a81aa6ab8a848f2f3c`，
[Actions 34008782134](https://github.com/simonlin1212/Vibe-Research/actions/runs/34008782134)。
在 PowerShell 7 父进程和 Node test runner 内分别复现：Windows PowerShell 5.1 的
Get-Acl 因继承 PSModulePath 而无法加载 Security 模块；仅清除该变量后同一 ACL 操作成功。
另证实 PATH 为空时找不到 powershell.exe，只补 SystemRoot 也不足以正常运行替身。
因此权限入口重置该模块搜索变量，测试环境补充明确白名单内的 Windows 系统/用户目录变量；
不把 API key、OAuth token 或模型配置并入测试环境，不降低 DACL 要求。

Windows 还暴露出真实收尾缺陷：临时目录删除 EPERM 会在 close 回调中抛未捕获异常，
原 Promise 无法正常兑现。新增故障注入测试先复现，再加有界删除重试和显式 agent_cleanup_failed；
清理失败不冒充成功，错误不回显目录或原始异常文本。24 项本地运行器测试通过，独立复审无可行动 P1/P2。

第四轮 `0a3d6c11d51481ac8eee6b274990df409a5c506a`，
[Actions 34009031815](https://github.com/simonlin1212/Vibe-Research/actions/runs/34009031815)：
Windows Node 24 原生契约测试 73 项，69 通过、3 失败、1 POSIX 专属跳过。
ACL、登录探针、清理与进程终止已通过。三个失败均明确显示空的 `--tools` 参数被吞，
后面的 `--strict-mcp-config` 被当作值；独立诊断也显示 JSON 引号丢失。
这不是“Windows 只需调一下测试”，而是实际参数完整性缺陷，不得放宽安全断言。

第五轮改为识别标准 npm Node 启动模板，直接调用当前后端 Node 和同一 CLI 脚本，原样传 argv。
参照本机 npm 的 read-cmd-shim / cmd-shim 实现；没有引入新依赖，不解析任意 PowerShell，
不经 cmd.exe 拼接、不用字符串转义补丁掩盖旧 PowerShell 重解析。
原生 exe 与 WorkBuddy 内置 JS 路径保持支持；未知/自定义 ps1 明确拒绝，要求官方原生或标准 npm CLI。
本地选择的 CLI 仍是可信可执行代码，此解析器不是脚本安全审计或沙箱。
提前验证启动器，拒绝时不占并发槽位；Windows 回归连续五次拒绝，不能在第五次变成 busy。

真实子进程逐字核对空串、JSON、中文、引号、换行、路径反斜杠及元字符；另由实际 npm cmd-shim
生成启动器再验证入口、参数及 exit 7，证明不是只对手写替身成立。31 项本地定向回归
30 通过、1 Windows ACL 跳过，类型检查通过，两轮独立增量复审均无可行动 P1/P2。
临时云端诊断脚本及 workflow 步骤已移除，证据保留在历史提交与本地日志，正式测试断言保留。
第五轮提交 `1734fbed7fb5c1c6ef0302a9d9aef60b274f4b18`，
[Actions 34009395352](https://github.com/simonlin1212/Vibe-Research/actions/runs/34009395352)，六组合最终全部成功。
完整云端输出保留为 `.local/reviews/m19-final-ci.log`，失败轮日志也保留，不删失败证据。

矩阵为 Windows / macOS / Ubuntu × Node 22 / 24，Python 3.12。
Windows 使用既有原生契约测试集合、类型检查、前端测试/构建、Python 测试和 PowerShell 入口语法解析；
macOS / Ubuntu 运行后端全量。测试替身不会调用 WorkBuddy、Claude 或 Codex 订阅模型，不消耗模型额度。
本批云端验证不等同实体 Windows 客户电脑的交互验收，也不证明该平台上的真实订阅研究全流程。

## 尚未覆盖

- Windows 实体客户机、浏览器人工交互及真实订阅六阶段；本批不给云端注入个人登录态。
- Claude/WorkBuddy 最新补丁后的 full 长测及其他 API 厂商授权矩阵，仍按 M18 边界单列。
- M18 的 117 端点体检仍为 89 ok / 9 partial / 19 failed，不因离线 CI 通过而改写可用性。
- 当前测试分支不是 main 的发布候选合并；合并、正式整理开发分支、tag、Release 仍需另行授权。

## 交付自评与后续改进

按 agent-self-evaluation 的五轴独立自评，整体 **4.0/5**：

- 准确性 4：六组合 headSha / 日志 / 跳过项均核对；真实 Windows 客户进程行为仍不能由替身证明。
- 完整性 4：本批独立分支和原生云端检查闭环；Windows 后端采用既有原生契约集，尚非全 813 项。
- 清晰度 4：首节集中交付结论与边界；逐轮正文保留大量工程细节，仅供追溯。
- 可操作性 4：测试分支与固定运行链接可复验；真实厂商授权与客户交互仍需相应环境。
- 简洁度 4：用户总结保持简短，完整证据分流到本报告；历史失败轮较长但不删除失败记录。

改进优先级：① 在具备受支持环境后补 Windows 真实引擎/客户交互，不往公共 CI 注入个人登录态；
② 扩大 Windows 后端原生测试覆盖时逐项适配平台假设，不以跳过失败凑通过。
用户是否会同意：预计会，本报告交付可查的云端结果，但明确没有把它升级为客户完整版或已发布。

技能影响：terminal-ops 保留活动 checkout 和真实 index；github-ops 约束独立分支及只读权限的 CI；
codex-delegate 用实际独立 Codex 逐批复审并核实误报；agent-self-evaluation 检查交付范围与证据缺口。
