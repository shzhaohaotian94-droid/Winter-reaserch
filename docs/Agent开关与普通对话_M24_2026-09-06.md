# M24 · 左上角 Agent 开关与默认普通对话

## 用户决定

2026-09-06：将“开启Agent”放到左上角实际 AI 来源旁，默认关闭，解决简单问题进入多步研究的问题。
本决定取代 M5–M23 的默认开启口径；M23c 暖橙品牌、原侧栏、首页交流框与五分类紧凑布局保留。

## 行为与边界

- 左上角与设置页共用一个开关。未连接时不可开启；关闭时首页显示“普通对话 · Agent 已关闭”。
- 旧平铺/v2 自动开启配置按关闭读取，不在读取时修改密钥存储；明确切换写入偏好标记，刷新后保留。
  新来源默认关闭，同一来源重新测试/保存保留明确选择。
- 关闭时仍用选定来源：已验证 API 使用独立直连；Codex/Claude/WorkBuddy 订阅及其他 Responses 来源
  进入无工具、非持久会话的单轮内核，不创建聊天研究 MCP bridge，不运行研究工具循环，超时 120 秒。
  不转换订阅凭据、不伪造 API 直连能力、不使用后台默认来源兜底。标题翻译沿用同一来源的无工具通道。
- 开启时保留 M22 联网、数据、计算和任务工具。六阶段研究、辩论、Agent 回测及资料转写仍要求开启。
- 模式切换会取消在途聊天和前置订阅检查；取消不误报登录/连接失败，已开始的独立研究任务不随开关停止。
- 普通对话不联网，不声称获取了最新数据；问题需要实时证据时提示开启 Agent。保留资料引用与产出检查。
- 兼容边界：浏览器所有请求显式发送模式，缺少浏览器配置不借后台 AI；旧 CLI/API 未传 executionMode
  的兼容契约仍默认为 agent，本次没有迁移第三方 API 调用方。不要混同为浏览器默认开启。

## 验证

- 前端 70/70；前后端类型检查通过；Vite 构建通过，保留既有 >500 kB 分块警告。
- 后端全套串行 825 项：824 通过、1 Windows ACL 在 macOS 跳过。包含真实 service 调用路径、
  Codex SDK 替身验证订阅/Responses 来源不挂研究工具、未知来源拒绝，以及已有 Agent 业务的回归。
- 先红后绿：旧默认开启与订阅禁止关闭用例失败后修复；补取消用例抓到通用请求层将 AbortError
  错转成网络故障，两用例修后通过。未把中间失败轮次记成成功。
- 真实 Chrome 1373px：开关位于 AI 状态右侧；开→刷新仍开→关，最终 false；实际来源仍 Claude订阅，
  原聊天保留，页面无横向溢出。截图 `/tmp/m24-switch-final.png` 为本机临时证据，不进公开产物。
- 新后端真实 Claude 订阅普通对话：独立 session `m24-light-probe`，问题“用一句话解释什么是现金流。”，
  HTTP 200，墙钟 4219 ms，单句有效回答，无工具记录。没有往用户首页历史注入测试消息。
  该耗时仅代表本次请求，不承诺固定延迟；订阅仍有客户端启动与模型服务耗时。
- 实际 Codex CLI / GPT-5.6 Sol 三轮增量审查：R1 两项采纳（同来源重存保持偏好、连接预检可取消），
  一项未采纳（全局旧 API 默认迁移，理由见兼容边界）；R2 取消错误分类问题由新增测试同时复现并修复；
  R3 `No actionable P1/P2 findings`。产物 `.local/reviews/m24-agent-toggle*.md`。
- 本轮未使用 WorkBuddy 额度，未真跑所有其他供应商、Windows 实机或新的六阶段 full。

## 本地交付

唯一源码 `vibe-research-agent-Codex/`，`feat/dual-engine @ 2ed80acd` 不变。
UI `http://127.0.0.1:5940/`，API `127.0.0.1:8765` 已重启加载本轮代码；5930 的项目 39 未动。
没有复制登录态、清空历史或修改个人全局模型配置；没有提交、推送、打标签或发布。未新增全局记忆便笺。

## 自查

按 agent-self-evaluation：总评 4.0/5，可交付本地使用。

| 维度 | 分数 | 证据与后续改进 |
|---|---|---|
| 准确性 | 4 | Claude 实测 4.2 秒、无研究工具；不将单次延迟推广为性能保证，可在后续积累延迟分布 |
| 完整性 | 4 | 位置、默认、持久化、取消和来源保持覆盖；后续有额度再做其他订阅真实复验 |
| 清晰度 | 4 | 普通对话与 Agent 标题已区分；后续可观察用户是否理解单轮对话的上下文边界 |
| 可操作性 | 4 | 原网址刷新可用；尚非公开版本，不宣称外部用户已获得更新 |
| 简洁度 | 4 | 复用同一开关和无工具内核；兼容旧 API 的区别仍需在开发文档解释 |

无严重缺口。自问用户是否认可：功能对应截图位置与默认关闭要求，实测普通问题得到简短回答；最终视觉以用户打开验收为准。
